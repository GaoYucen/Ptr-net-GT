# ana0719：动态特征独立投影与一致性连续指标复测

## 1. 本轮目标

本轮基于 `ana0718.md` 的后续建议，重点复测两项措施：

1. **动态特征单独投影**是否比直接拼接更有效；
2. **一致性评估细化**后，是否能说明此前 `permutation_consistency = 0.0` 主要是指标过严，而不是模型完全不等变。

本轮工作范围仍限定在 TSP-only 主线，主要涉及：

- `ptrnet_gt/models/component_merge_decoder.py`
- `ptrnet_gt/models/factory.py`
- `scripts/evaluate.py`
- `tests/test_component_merge_decoder.py`

---

## 2. 本轮代码改动

## 2.1 动态特征独立投影开关

在 `ComponentMergeDecoder` 中加入以下可配置项：

- `use_role_feature_projection`
- `role_embedding_dim`
- `use_distance_projection`
- `distance_embedding_dim`

对应实现：

```python
role_emb = role_mlp(role_features)
distance_emb = distance_mlp(dist)
```

并将原先的边特征拼接从“直接拼 raw role features + raw distance”扩展为：

```python
[
    h_u,
    h_v,
    h_u * h_v,
    torch.abs(h_u - h_v),
    role_emb_u / role_feat_u,
    role_emb_v / role_feat_v,
    distance_emb / dist,
]
```

这样可以直接做如下对照：

- `dynamic raw`：动态特征直接拼接
- `dynamic projected`：动态特征与距离先投影再拼接

---

## 2.2 一致性评估补充连续指标

在 `scripts/evaluate.py` 中，保留原有：

- `permutation_consistency`

并新增：

- `permutation_exact_successor_consistency`
- `permutation_edge_jaccard`
- `permutation_edge_recall`
- `permutation_relative_length_diff`
- `permutation_action_prob_equiv_error`

其中最后一个指标是对 `joint_edge` 单步概率在反置换后的平均绝对误差。

---

## 2.3 单步 logits 等变测试

在 `tests/test_component_merge_decoder.py` 新增：

1. 投影版 joint-edge 前向 shape 测试；
2. 未训练模型下单步 `joint_edge` log-prob 反置换误差测试。

核心检查是：

```python
log_p(X, state)
vs
log_p(PX, Pstate) 反置换回原编号
```

并验证两者概率差最大值足够小。

---

## 3. 基础单测结果

运行命令：

```bash
KMP_DUPLICATE_LIB_OK=TRUE /opt/conda/envs/py11/bin/python -m unittest \
  tests.test_component_merge_decoder \
  tests.test_component_merge_state
```

结果：

- `Ran 14 tests in 0.160s`
- `OK`

说明：

- 动态特征投影路径已接通；
- 新增单步等变测试可通过；
- 当前 joint-edge 主链路未被破坏。

---

## 4. 复测设置

### 4.1 训练设置

- TSP20
- `joint_edge`
- `training.objective=equiv_set`
- `training.baseline=none`
- `epochs=1`
- `epoch_size=1024`
- `batch_size=64`
- `val_size=256`

### 4.2 对比组

#### A. dynamic raw

- `model.use_dynamic_role_features=true`
- `model.use_role_feature_projection=false`
- `model.use_distance_projection=false`

#### B. dynamic projected

- `model.use_dynamic_role_features=true`
- `model.use_role_feature_projection=true`
- `model.role_embedding_dim=32`
- `model.use_distance_projection=true`
- `model.distance_embedding_dim=16`

---

## 5. 训练结果

### 5.1 验证集结果

| 方法 | validation avg_cost | supervised loss |
|---|---:|---:|
| dynamic raw | 4.6009 | 0.9291 |
| dynamic projected | 4.6554 | 0.7183 |

### 5.2 观察

有一个很典型的现象：

- **projected 版 loss 更低**；
- 但 **projected 版验证 tour cost 没有优于 raw 版**。

这说明：

1. 独立投影确实改变了优化景观；
2. 但在当前 1 epoch / 1024 samples 的 smoke test 下，它还没有转化为更好的路线质量；
3. 因此目前只能说“投影版可训练且损失下降更快”，还不能说它在最终性能上有效。

---

## 6. 测试集评估结果

运行评估命令后，得到：

| 方法 | mean tour length | feasible rate | throughput (inst/s) | parameter_count |
|---|---:|---:|---:|---:|
| dynamic raw | 4.6219 | 1.0 | 4430.16 | 627,841 |
| dynamic projected | 4.6721 | 1.0 | 4482.62 | 637,553 |

### 6.1 结论：动态特征独立投影

在本轮 smoke test 中：

- projected 版参数更多；
- 路线质量略差于 raw 版；
- 吞吐没有明显恶化。

因此当前最稳妥结论是：

> **动态特征独立投影的工程实现已打通，但在当前小预算 TSP20 复测中，还没有体现出优于直接拼接的性能收益。**

这与 `ana0718` 中“动态特征暂未显示稳定收益”的判断一致，只是现在把“raw vs projected”也显式测了一遍。

---

## 7. 一致性连续指标结果

### 7.1 评估输出

#### dynamic raw

- `permutation_consistency = 1.0`
- `permutation_exact_successor_consistency = 1.0`
- `permutation_edge_jaccard = 1.0`
- `permutation_edge_recall = 1.0`
- `permutation_relative_length_diff = 0.0`
- `permutation_action_prob_equiv_error = 1.057e-09`

#### dynamic projected

- `permutation_consistency = 1.0`
- `permutation_exact_successor_consistency = 1.0`
- `permutation_edge_jaccard = 1.0`
- `permutation_edge_recall = 1.0`
- `permutation_relative_length_diff = 0.0`
- `permutation_action_prob_equiv_error = 1.127e-09`

### 7.2 解释

这是本轮最关键的结果。

在修正评估脚本中的反置换映射方向后，出现了更一致的结果：

- 完全 successor 一致率 = 1.0；
- 边集合 Jaccard = 1.0；
- 边恢复率 = 1.0；
- 路线长度相对差 = 0；
- 单步动作分布等变误差约为 `1e-9`。

这说明：

1. **模型在 successor、边集合与路径长度层面都完全一致**；
2. **模型在单步 joint-edge 概率层面几乎精确等变**；
3. 之前的 `0.0` 实际上主要来自**评估脚本中的反置换映射方向错误**；
4. 因此此前不能据此断言模型完全不等变，反而应该优先排查实现细节。

换句话说，本轮复测基本验证了原建议中的判断：

> **此前的一致性 0.0 不是模型失去等变性的证据，主要是评估实现问题；修正后可以看到当前 joint-edge 实现在线路、边集合和单步概率层面都高度等变。**

---

## 8. 单步 logits 等变测试带来的额外结论

由于新增了未训练模型的单步测试，并且测试通过，因此现在可以进一步缩小问题范围：

- 如果未训练模型单步 logits 本身就接近严格等变；
- 而修正后的连续一致性指标也几乎完美；

那么：

> 后续若再看到“整条 tour exact consistency 很低”，应优先怀疑 **评估实现、反置换映射或比较方式**，而不是直接认为模型结构不等变。

这一步非常重要，因为它说明此时继续盲目加 consistency regularization，未必是最优先事项。

---

## 9. 阶段性结论

## 9.1 动态特征独立投影：当前未验证出收益

本轮可以确认：

- 投影版实现没问题；
- 投影版可稳定训练；
- 投影版训练损失更低；
- 但在当前 smoke test 下，没有带来更好的 TSP20 路线质量。

因此当前结论是：

> **该措施“可用”，但暂未证明“有效”。**

## 9.2 一致性连续指标与评估排查：有效，且很关键

本轮最有信息量的收获是：

- 新增连续指标后，首先暴露了旧结论与边集合/长度结果之间的不自洽；
- 随后定位到评估脚本中的反置换方向错误；
- 修正后，`exact successor consistency` 与其他连续指标全部一致为近乎完美。

因此当前应把结论更新为：

> **此前的一致性 0.0 主要是评估实现错误造成的假象；修正后，当前 joint-edge 模型在这组测试上表现出非常强的置换等变性。**

---

## 10. 下一步建议

### 10.1 优先级一：修订一致性主指标

后续建议：

- 保留 exact consistency 作为最严格指标；
- 但报告时至少同时展示：
  - edge jaccard
  - edge recall
  - relative length diff
  - action prob equiv error

也就是说，今后**不要再单独依赖单一指标，更要先确认反置换实现本身正确**。

### 10.2 优先级二：更大预算复测 projected dynamic

建议在更大预算下再测：

- `epochs=5~10`
- 多个 seed
- `equiv_set` 路线固定

这样才能更公平地判断：

- raw dynamic
- projected dynamic

到底谁更优。

### 10.3 优先级三：评估 consistency regularization 是否还有必要加强

如果单步概率已经天然接近等变，那么下一步一致性路线更应该关注：

- 结构化比较方式；
- 多步解码中的 canonicalization；
- successor 表示的不变比较。

而不一定是继续增强 logits/prob-level 正则本身。

---

## 11. 一句话结论

截至 0719 这轮复测：

> **动态特征独立投影已经实现并可稳定训练，但在当前 TSP20 smoke test 中尚未优于直接拼接；而新增连续一致性指标加上未训练单步等变测试进一步帮助定位出：先前的 `permutation_consistency = 0.0` 主要来自评估脚本反置换实现错误，修正后当前 joint-edge 模型表现出接近严格的置换等变性。**
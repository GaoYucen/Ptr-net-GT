# ana0718：计算群论启发的 component-merge 路线阶段性实验记录

## 1. 目标与背景

本轮工作的目标是结合：

- `参考材料/NIPS_计算群论论文思路.pdf`
- `参考材料/计算群论启发的路径分量并合神经路由_论文原理说明.md`
- 当前 `Ptr-net-GT` 的 TSP-only 代码

验证以下几个问题：

1. 路径分量并合解码中的 **joint_edge** 是否优于原有 **tail_head** 两阶段动作；
2. 显式加入 **动态分量/节点角色特征** 是否能进一步提升表现；
3. **等价动作集合监督（equiv_set）** 是否优于固定顺序监督（fixed_order）；
4. **节点重编号一致性评估** 与 **consistency regularization** 是否能带来可观测收益。

本文件记录本轮已经完成的代码改动、实验命令、实验结果与阶段性结论。

---

## 2. 已完成的代码改动

## 2.1 `joint_edge` 联合选边模式

修改文件：

- `ptrnet_gt/models/component_merge_decoder.py`
- `ptrnet_gt/models/factory.py`
- `configs/component_merge/tsp20.yaml`
- `configs/component_merge/tsp50.yaml`
- `configs/component_merge/tsp100.yaml`

### 实现内容

为 `ComponentMergeDecoder` 增加：

- `action_mode="tail_head"`
- `action_mode="joint_edge"`

其中 `joint_edge` 的核心做法是：

1. 通过 `ComponentMergeState.get_edge_mask()` 得到合法边集合；
2. 对每个候选边 `(u,v)` 构造边特征；
3. 用 `edge_score` MLP 对边直接打分；
4. 在合法边上 softmax；
5. 直接选择边，而不是先选 Tail 再选 Head。

### 当前静态边特征

基础版使用：

- `h_u`
- `h_v`
- `h_u * h_v`
- `|h_u - h_v|`
- `dist(u,v)`

### 接口兼容修复

由于现有训练代码默认按 token 计算 log-likelihood，而 `joint_edge` 是在边空间上输出概率，因此修复了：

- `joint_edge` 下 `(tail, head) -> edge_idx = tail * n + head` 的 log-likelihood 映射。

---

## 2.2 动态分量特征接入与开关化

修改文件：

- `ptrnet_gt/states/component_merge.py`
- `ptrnet_gt/models/component_merge_decoder.py`
- `ptrnet_gt/models/factory.py`

### 新增状态接口

在 `ComponentMergeState` 中新增：

```python
get_node_role_features()
```

当前输出 8 维节点动态角色特征：

1. `is_isolated`
2. `is_start`
3. `is_end`
4. `is_internal`
5. `component_size / n`
6. `step / n`
7. `in_degree`
8. `out_degree`

### 开关设计

增加配置：

```yaml
model:
  use_dynamic_role_features: true | false
```

这样可以严格比较：

- `joint_edge(static)`
- `joint_edge(dynamic)`

默认可关闭，后续实验中为了隔离变量，很多对比都在 `false` 下完成。

---

## 2.3 等价动作集合监督（最小可运行版）

新增文件：

- `ptrnet_gt/training/supervised.py`

修改文件：

- `ptrnet_gt/training/__init__.py`
- `scripts/train.py`

### 训练目标支持

当前已支持：

- `training.objective=reinforce`
- `training.objective=fixed_order`
- `training.objective=equiv_set`

### 当前标签来源

为了快速验证思路，暂时没有外接最优解标签，而是：

1. 用 `nearest_neighbor_multistart_tour` 生成启发式目标 tour；
2. 将其转成目标边集；
3. 在 `joint_edge` 上定义监督。

### fixed_order

固定唯一目标边顺序，逐步做交叉熵式监督。

### equiv_set

在每一步，把所有“当前仍合法且属于目标边集”的边都视为正例，损失为：

```text
-log sum_{e in legal_target_edges} p(e | s)
```

这正对应论文思路中的“等价动作集合监督”。

---

## 2.4 节点重编号一致性评估

修改文件：

- `scripts/evaluate.py`

新增指标：

```json
"permutation_consistency": ...
```

### 评估方法

对同一 batch：

1. 随机生成节点置换 `P`；
2. 原图上跑模型得到 `pi`；
3. 置换图上跑模型得到 `pi_perm`；
4. 反置换 `pi_perm` 回原编号；
5. 比较 successor 结构是否完全一致。

当前指标比较严格：

> 只有 successor 全一致才记为 1，否则为 0。

---

## 2.5 consistency regularization（最小版）

修改文件：

- `ptrnet_gt/training/supervised.py`
- `scripts/train.py`

### 新增配置

```yaml
training:
  consistency_weight: 0.0
```

### 训练逻辑

在 supervised 训练时：

1. 对原始 batch 和随机重编号 batch 分别编码；
2. 在每一步计算 joint-edge 分布；
3. 将置换图上的边分布反置换回原编号空间；
4. 在有效 mask 区域对概率做归一化；
5. 用 **masked probability MSE** 作为一致性损失。

### 备注

第一版直接在带 `-inf` 的 log 概率上做 KL，训练时出现 NaN；
后续改成 masked probability MSE 后，训练已稳定可跑。

---

## 3. 运行环境约定

本轮实验使用服务器 `py11` 环境运行，且已将约定写入 `.clinerules`：

```bash
KMP_DUPLICATE_LIB_OK=TRUE /opt/conda/envs/py11/bin/python ...
```

已确认：

- Python: `/opt/conda/envs/py11/bin/python`
- Torch: `2.5.1+cu121`

---

## 4. 基础回归测试

运行命令：

```bash
KMP_DUPLICATE_LIB_OK=TRUE /opt/conda/envs/py11/bin/python -m unittest \
  tests.test_component_merge_decoder \
  tests.test_component_merge_state
```

结果：

- 早期 joint_edge 接入后：`Ran 10 tests, OK`
- 动态特征与后续改造后：`Ran 12 tests, OK`

说明当前代码在状态更新、joint_edge 路径、角色特征接口等方面，至少在单测层面保持可运行。

---

## 5. 实验结果汇总

## 5.1 `tail_head` vs `joint_edge`（REINFORCE smoke test）

### 设置

- TSP20
- 1 epoch
- `epoch_size=2048`
- `batch_size=128`
- 随机生成验证/测试集

### 训练后验证结果

| 方法 | validation avg_cost |
|---|---:|
| tail_head | 6.6503 |
| joint_edge (static) | 4.6432 |

### 评估结果

| 方法 | mean tour length | feasible rate | throughput (inst/s) |
|---|---:|---:|---:|
| tail_head | 6.5649 | 1.0 | 349.28 |
| joint_edge (static) | 4.5626 | 1.0 | 421.40 |

### 结论

`joint_edge` 明显优于 `tail_head`，说明：

> 将动作直接建模为边 `(u,v)`，比拆成 `p(tail)p(head|tail)` 更符合 TSP 的代价结构。

---

## 5.2 `joint_edge(static)` vs `joint_edge(dynamic)`

### 设置

- TSP20
- 1 epoch
- `epoch_size=2048`
- `batch_size=128`
- 随机生成验证/测试集

### 评估结果

| 方法 | mean tour length | feasible rate | throughput (inst/s) |
|---|---:|---:|---:|
| joint_edge (static) | 4.5626 | 1.0 | 421.40 |
| joint_edge (dynamic) | 4.6949 | 1.0 | 386.12 |

### 结论

动态分量特征的接口已经打通，但在这次**极小预算 smoke test** 中，没有立刻优于 static 版，反而略差。

这不能说明动态特征无效，只能说明：

- 训练预算太小；
- 参数略增；
- 更复杂特征可能需要更长训练才显现收益。

因此后续实验中，暂时将动态特征关掉，用于隔离 `equiv_set` 与 consistency 路线的效果。

---

## 5.3 `fixed_order` vs `equiv_set`（关闭动态特征）

### 设置

- `joint_edge`
- `model.use_dynamic_role_features=false`
- `training.baseline=none`
- `training.epochs=1`
- `epoch_size=1024`
- `batch_size=64`
- 启发式目标：`nearest_neighbor_multistart_tour`

### 训练后验证结果

| 方法 | validation avg_cost | supervised loss |
|---|---:|---:|
| fixed_order | 4.8358 | 3.6670 |
| equiv_set | 4.3639 | 0.7969 |

### 评估结果

| 方法 | mean tour length | feasible rate | throughput (inst/s) | permutation_consistency |
|---|---:|---:|---:|---:|
| fixed_order | 4.7973 | 1.0 | 413.48 | 0.0 |
| equiv_set | 4.4136 | 1.0 | 400.55 | 0.0 |

### 结论

这是当前最强、最清晰的正向实验结果：

> **在 joint_edge 且关闭动态特征时，equiv_set 监督明显优于 fixed_order 监督。**

这直接支持了论文原理说明中的核心判断：

> 对同一目标 tour，不应只把唯一固定构造顺序当作正例；允许所有当前合法目标边共同成为正例，会显著改善学习效果。

---

## 5.4 `equiv_set` vs `equiv_set + consistency regularization`

### 设置

- `joint_edge`
- `model.use_dynamic_role_features=false`
- `training.objective=equiv_set`
- `training.consistency_weight=0.1`（正则版）
- `epochs=1`
- `epoch_size=1024`
- `batch_size=64`

### 训练后验证结果

| 方法 | validation avg_cost |
|---|---:|
| equiv_set | 4.3639 |
| equiv_set + consistency | 4.3770 |

### 评估结果

| 方法 | mean tour length | feasible rate | throughput (inst/s) | permutation_consistency |
|---|---:|---:|---:|---:|
| equiv_set | 4.3606 | 1.0 | 349.40 | 0.0 |
| equiv_set + consistency | 4.3995 | 1.0 | 422.01 | 0.0 |

### 结论

第一版 consistency regularization 已经：

- 能稳定训练；
- 不再出现 NaN；
- 能在训练链路中运行。

但当前 smoke test 下它还没有带来：

- 更好的 tour length；
- 更高的 `permutation_consistency`。

因此现阶段只能说：

> **一致性正则的工程入口已接通，但这版最小实现还没有产生可观测收益。**

---

## 6. 阶段性分析结论

结合本轮所有实现与 py11 实际实验，可以给出如下阶段性结论。

## 6.1 已被实验证明有效的部分

### A. `tail_head -> joint_edge`：有效

这是第一层最明确的收益。

说明：

- 对 TSP 而言，动作对象天然是边；
- 直接建模边比分两阶段建模 Tail/Head 更合理；
- 在当前 component-merge 框架下，这一步带来显著收益。

### B. `fixed_order -> equiv_set`：有效，且是当前最强信号

这是第二层、也是最符合“计算群论启发”本意的收益。

说明：

- 同一目标回路对应多个合法构造顺序；
- 固定顺序监督确实会制造标签冲突；
- 允许所有合法目标边共同作为正例，可以明显改进训练结果。

这也是当前整条研究路线中最值得优先继续深入验证的核心结论。

---

## 6.2 已接通但尚未显示稳定增益的部分

### A. 动态分量特征

当前状态：

- 接口已打通；
- 开关已可控；
- smoke test 下暂无稳定增益。

这并不意味着无效，只是说明当前预算不足以判断。

### B. permutation consistency regularization

当前状态：

- 评估指标已可运行；
- 正则训练已可运行；
- 数值问题已解决；
- 但当前 `permutation_consistency` 仍是 0.0。

这说明：

- 我们已经把“一致性问题”测出来了；
- 但这版最小正则还没有真正解决它。

---

## 6.3 对“计算群论方法是否有帮助”的当前回答

基于本轮实验，当前最合理的回答是：

> **有帮助，但最先被证明有帮助的不是抽象群论模块本身，而是由群作用/等价类思想导出的训练与解码设计。**

具体来说：

1. **joint_edge** 对应“把动作直接建模为边/置换作用”的视角；
2. **equiv_set** 对应“同一目标存在多个等价合法构造顺序”的视角；
3. **permutation consistency** 对应“节点重编号等变性”的视角；
4. **dynamic component features** 对应“分量结构进入状态表示”的视角。

而在这四者中，目前最有实证支撑的是前两者，尤其是 **equiv_set**。

---

## 7. 当前存在的问题与局限

## 7.1 标签来源仍然较弱

目前 supervised 实验使用的是：

- `nearest_neighbor_multistart_tour`

生成的启发式目标 tour，而不是最优标签或更强参考解。

这意味着当前结论是：

> `equiv_set` 比 `fixed_order` 更适合学习“启发式目标边集”。

要进一步增强说服力，需要：

- 更高质量标签；
- 或小规模精确解；
- 或离线高质量参考解集。

## 7.2 consistency 指标比较严格

当前 `permutation_consistency` 要求：

- 反置换后的 successor **完全一致**。

这是一个非常严格的判据，因此早期很容易得到 0.0。

后续可以考虑加入更细粒度指标，例如：

- 边集合一致率；
- top-k 候选边一致率；
- 概率矩阵相关性；
- 不同起点/方向 canonicalized 后的一致率。

## 7.3 当前训练预算很小

本轮大量实验都只是 smoke test：

- 1 epoch
- 1024 / 2048 samples

它们足够验证“方向能否跑通”，但不足以做最终性能结论。

---

## 8. 后续建议优先级

## 8.1 第一优先级：把 `equiv_set` 做扎实

这是当前最有实证价值的方向，建议下一步优先做：

1. 多 seed；
2. 更大训练预算；
3. TSP20 训练、TSP50 测试；
4. 汇总平均值与方差；
5. 对比：
   - `tail_head + reinforce`
   - `joint_edge + reinforce`
   - `joint_edge + fixed_order`
   - `joint_edge + equiv_set`

## 8.2 第二优先级：改进 consistency 设计

当前正则只是第一版最小实现，后续可考虑：

- 只对目标合法边子空间做一致性约束；
- 直接对 masked logits / masked probs 做更合适的对齐；
- 引入 canonical edge ordering；
- 或对 successor / edge matrix 的结构一致性做更强监督。

## 8.3 第三优先级：重新评估动态分量特征

建议在 `equiv_set` 路线下再做：

- `joint_edge + equiv_set + static`
- `joint_edge + equiv_set + dynamic`

这样更容易判断动态特征到底是帮助还是干扰。

---

## 9. 一句话结论

截至 0718 这轮实验：

> **最被实验证明有效的“计算群论启发”改动，是 joint_edge 解码与 equiv_set 等价动作集合监督；而动态分量特征与 permutation consistency 正则的工程接口已打通，但在当前 smoke test 下尚未显示稳定收益。**

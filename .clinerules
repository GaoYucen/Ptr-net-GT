# Ptr-net-GT 项目运行规则

## 1. 当前研究范围

- 当前仓库按 **TSP-only** 维护。
- 新功能、修复和重构应优先围绕：
  - `ptrnet_gt/states/component_merge.py`
  - `ptrnet_gt/models/component_merge_decoder.py`
  - `scripts/train.py`
  - `scripts/evaluate.py`

## 2. 运行环境约束

当前 macOS + PyTorch 环境存在 OpenMP 冲突；直接导入 `torch` 或运行测试时，可能触发：

```text
OMP: Error #15: Initializing libomp.dylib, but found libomp.dylib already initialized.
```

因此，**所有需要导入 `torch` 的命令**，默认都应带上：

```bash
KMP_DUPLICATE_LIB_OK=TRUE
```

例如：

```bash
KMP_DUPLICATE_LIB_OK=TRUE python scripts/train.py --config configs/component_merge/tsp20.yaml
KMP_DUPLICATE_LIB_OK=TRUE python scripts/evaluate.py --config configs/component_merge/tsp20.yaml --checkpoint outputs/component_merge_tsp20/model.pt
KMP_DUPLICATE_LIB_OK=TRUE python -m pytest tests/test_component_merge_state.py -q
```

## 3. 命令执行约定

- 若命令会触发 `torch` 导入，优先显式写出 `KMP_DUPLICATE_LIB_OK=TRUE`。
- 即使脚本内部已设置该环境变量，外部命令也建议保留此前缀，确保行为一致。

## 4. 长命令与引号安全规则

- 在执行 shell 命令时，**禁止输出未闭合的双引号或单引号**；否则终端可能进入续输状态，并出现 `dquote>` 之类提示。
- 对于可直接写成单行的命令，优先使用**不带多余引号的单行命令**，避免为了包裹整条命令再额外套一层 `"..."`。
- 当命令过长时，优先采用以下更安全的写法，而不是拼接一整段复杂引号：
  1. 使用反斜杠续行，例如：

  ```bash
  KMP_DUPLICATE_LIB_OK=TRUE python scripts/train.py \
    --config configs/component_merge/tsp20.yaml \
    --epochs 1
  ```

  2. 若命令包含很多参数、JSON、正则或嵌套引号，优先先写入脚本文件，再执行脚本。
  3. 若必须带引号，优先使用**成对且易检查**的引号；能不用引号就不用，能用单引号包裹简单字面量时，不用双引号嵌套双引号。
- 对包含路径的命令，若路径中有空格，优先对**具体路径片段**加引号，而不是给整条命令外包一层大引号。
- 对 `heredoc`（如 `cat <<EOF`、`python - <<'PY'`）默认保持谨慎；这类写法一旦结束标记、引号或缩进有误，终端容易进入 `heredoc>` / `cmdand heredoc>` 续输状态。
- 需要写多行内容时，优先使用 `apply_patch` 直接改文件，或先生成脚本文件再执行；**不要**为了省事在终端里临时拼复杂 heredoc。
- 若必须使用 heredoc，结束标记必须**顶格、独占一行、前后无空格、与起始标记完全一致**；生成前先自检，避免把后续正文误发进续输提示符。
- 生成命令前，应先自检以下几点：
  - 引号是否成对闭合；
  - 是否存在 `"`、`'`、括号、反斜杠混用导致的截断风险；
  - 是否可以改写为多行续行格式以降低出错概率。
- 如果命令预计会很长，优先选择：**短命令 + 续行**，或**脚本文件 + 执行**，避免在终端里直接粘贴超长单条复杂命令。

## 5. 清理与重构方向

- 不再恢复旧双端模型、旧状态和非 TSP 问题代码。
- 优先继续把旧根目录训练/评估逻辑迁入 `ptrnet_gt/` 包。
- 新增文档、脚本和测试时，应遵循当前 TSP-only 主线。
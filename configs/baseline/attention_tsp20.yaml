experiment:
  name: attention_tsp20
  seed: 1234

problem:
  name: tsp
  size: 20

model:
  name: attention
  embedding_dim: 128
  hidden_dim: 128

training:
  batch_size: 512
  epochs: 100
  learning_rate: 0.0001
  baseline: rollout

output:
  root: outputs
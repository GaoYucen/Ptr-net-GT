experiment:
  name: component_merge_tsp100
  seed: 1234

problem:
  name: tsp
  size: 100

model:
  name: component_merge
  embedding_dim: 128
  hidden_dim: 128
  context_mode: cross_step

training:
  batch_size: 512
  epochs: 100
  learning_rate: 0.0001
  baseline: rollout

output:
  root: outputs